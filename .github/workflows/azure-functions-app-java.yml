name: Deploy Java project to Azure Function App

permissions:
  contents: read
  id-token: write

on:
  workflow_dispatch:
    inputs:
      function_app_name:
        description: Nome exato da Function App no Azure (tem de bater com o pacote target/azure-functions/<nome>)
        required: true
        default: tc4fb-func

env:
  DISTRIBUTION: zulu
  JAVA_VERSION: "21"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # Secrets OIDC (AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID) no environment "Actions"
    environment: Actions
    env:
      AZURE_FUNCTIONAPP_NAME: ${{ inputs.function_app_name }}
    steps:
      - name: Checkout GitHub Action
        uses: actions/checkout@v4

      - name: Setup Java Sdk ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          distribution: ${{ env.DISTRIBUTION }}
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven

      - name: Build with Maven
        run: mvn -B package -Dquarkus.azure-functions.app-name=${{ env.AZURE_FUNCTIONAPP_NAME }}

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Functions (RBAC)
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: target/azure-functions/${{ env.AZURE_FUNCTIONAPP_NAME }}
